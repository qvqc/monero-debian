#!/usr/bin/make -f

%:
	dh $@ --parallel --buildsystem=cmake

override_dh_auto_configure:
	dh_auto_configure -- -DBUILD_SHARED_LIBS=ON -DBUILD_GUI_DEPS=ON -DARCH=default

override_dh_systemd_enable:
	dh_systemd_enable --no-enable
